# Go build output
/bin/
/dist/
*.exe
*.test
*.out
coverage.out
coverage.html

# PHP tooling
/vendor/
.php-cs-fixer.cache

# Local config / secrets (API keys live here)
*.local.toml
config.toml
.env
.env.local

# Editor / OS
.DS_Store
.idea/
.vscode/

# Agent-local working state (per-machine; never shared)
/.claude/settings.local.json
/.claude/worktrees/
/.claude/memory-*.json
/.claude/memory-*.json.bak-*

# Browser-automation artifacts from live UI verification (#87)
/.playwright-mcp/
/*.png

# SDD scratch
.superpowers/
/preloadd

# Plugin binary is built in CI into the src/ tree, never committed
src/usr/local/emhttp/plugins/watch-aware-preloader/preloadd

# Local-only working docs (design specs, plans, verification) - not published
docs/private/

# Dev-only JS tooling (ESLint, jsdom). The plugin ships no JS dependencies:
# its browser scripts are plain script-scope files loaded by a bare <script src>.
node_modules/
